services:
  server:
    container_name: web-bubble-sheet-server
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "18020:18020"
    volumes:
      - ./server/logs:/app/server/logs
